# منصة آلين 2.0.1 — النقطة الأولى: الإعلانات

1. من Supabase افتح SQL Editor وشغّل `RUN_ON_SUPABASE_v2_0_1_COMPLETE.sql` كاملًا.
2. الملف قابل لإعادة التشغيل؛ يحذف السياسات ذات الأسماء نفسها ثم يعيد إنشاءها، لذلك لا تحتاج حذف أي Policy يدويًا.
3. بعد نجاحه شغّل `CHECK_SUPABASE_READINESS_v2_0_1.sql` للتأكد من الجداول والدوال وBucket وسياسات البنرات.
4. ارفع ملفات النسخة كاملة إلى GitHub بدون خلطها مع إصدار قديم.
5. امسح كاش الموقع أو افتحه بتحديث إجباري.

الملف SQL ينشئ/يحدّث حقول البنرات، يفعّل القراءة العامة، وينشئ bucket `alin-files` وسياسات رفع صور البنرات داخل `banners/` للمدير فقط.
