# تقرير النقطة الأولى — الإعلانات وصور البنرات

## تم إصلاحه
- حذف كتلة `ALIN V96` القديمة من الحزمة الرئيسية.
- حذف `store/banners-final.js` و`store/banners-final.css`.
- اعتماد `store/banners.js` و`store/banners.css` فقط.
- نقل البنر مباشرة تحت رأس المتجر وقبل الأقسام في الكمبيوتر والموبايل.
- إضافة رفع صورة، رابط اختياري، تاريخ بداية ونهاية، ترتيب، تعديل، إخفاء وحذف.
- حذف صورة البنر من التخزين عند حذف البنر أو استبدال صورته.
- إضافة تجهيز bucket وسياسات Storage داخل ملف SQL الحالي، وإلغاء الاعتماد على ملف v49 القديم.
- تحديث Service Worker حتى لا يخزن ملفات البنر المحذوفة.

## الاختبارات
- فحص المسارات والملفات: ناجح.
- فحص JavaScript لملف البنرات: ناجح.
- اختبار متصفح حقيقي بمحاكاة بيانات البنر: ناجح.
- صورة البنر: ظهرت.
- الموقع قبل الأقسام: صحيح.
- واجهة إدارة البنرات: ظهرت وعملت.
- أخطاء Runtime أثناء الاختبار: صفر.

## ملاحظة صادقة
تم اختبار الواجهة والعمليات بمحاكاة اتصال قاعدة البيانات. تنفيذ رفع حي إلى مشروع Supabase يتطلب تشغيل ملف SQL وتسجيل دخول المدير على المشروع الفعلي؛ بيئة الفحص لم تستطع الوصول إلى نطاق Supabase مباشرة.

- إصلاح ملف SQL ليكون قابلًا لإعادة التشغيل دون خطأ `policy already exists`.
- تحديث فحص الجاهزية إلى v2.0.1 وإزالة الاستدعاء القديم غير الموجود `alin_raise`.

## تحديث Step 1.3 — فصل الصورة عن النص
- الصورة أصبحت قطعة مستقلة ولا يوجد عنوان أو وصف فوقها.
- عنوان الإعلان والوصف يظهران في بطاقة أسفل الصورة.
- ارتفاع بطاقة النص تلقائي حسب طول المحتوى دون حد ثابت.
- قياس الكمبيوتر 16:5، وقياس الموبايل 16:9 مع `object-fit: contain` لتجنب تشويه الصورة.
- رابط الإعلان يظهر كزر داخل بطاقة النص.
- تم تحديث كاش PWA لمنع بقاء التصميم السابق.
