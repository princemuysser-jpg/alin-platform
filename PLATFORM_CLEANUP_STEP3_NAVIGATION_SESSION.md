# ALIN v2.1.4 — تنظيف platform.js: التنقل والجلسة

تم نقل مسؤولية واجهة الدخول وفتح الصفحات وتسجيل الخروج وحماية صلاحيات الصفحات إلى `modules/core/navigation.js`.

التغييرات الفعلية:
- حذف `showLogin` و`doLogin` و`openPage` و`logout` من `platform.js`.
- حذف تسجيل دخول المدير المحلي القديم من `platform.js` واعتماد Supabase Auth فقط.
- حذف خمس طبقات قديمة كانت تعيد تعريف `openPage` داخل `platform.js`.
- منع `security.js` و`courier/dashboard.js` و`admin/dashboard.js` من تغليف `openPage` أو `logout` أو `doLogin`.
- جعل `cloud-status-ui.js` يقدم خدمة المصادقة فقط بدون استبدال وظائف الواجهة.
- توحيد صفحة المحاسب على المالية وصفحة المدير على لوحة المعلومات من خلال التنقل المركزي.
- تحويل حماية انتهاء الجلسة إلى أحداث واضحة بدون إعادة تعريف الوظائف.
- إضافة ملف التنقل إلى ترتيب الوحدات وذاكرة PWA.

النتيجة:
- مالك واحد فقط لكل من `openPage` و`logout` و`doLogin` و`showLogin`.
- حجم `platform.js` انخفض من 419128 إلى 416591 بايت.
- لا يوجد تحديث SQL جديد لهذه المرحلة.
